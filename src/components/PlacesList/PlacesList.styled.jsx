import styled from 'styled-components';
import * as colors from '../../assets/theme'

export const PlacesListContainer = styled.div`
  display: flex;
  width: auto;
  flex-direction: row;
  padding: 20px;
  gap: 1rem;
  background-color: ${colors.deepSea};
  color: ${colors.midnightBlue};
`;